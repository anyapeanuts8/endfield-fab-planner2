# エンドフィールド 工場レイアウト自動最適化アプリ 仕様書

**バージョン**: 1.1  
**作成日**: 2026-06-25  

---

## 1. アプリ概要

エンドフィールドの工場レイアウトを、設備間の接続関係（フロー図）から自動最適化するローカルWebアプリ。

ユーザーはブラウザ上でフロー図（どの設備とどの設備がどの輸送種別で繋がるか）を定義し、最適化エンジンがグリッド上の配置・配管ルート・電柱配置を自動生成する。

### 最適化目標（優先順位）

1. **全設備・電柱・ノードを含む外接矩形の面積最小化**
2. 同面積の場合、**電柱本数の最小化**

---

## 2. 技術スタック

| 要素 | 採用技術 |
|---|---|
| バックエンド | Python / FastAPI |
| フロントエンド | React + Canvas（または SVG） |
| 実行環境 | localhost のみ（ローカル実行） |
| データ保存 | サーバー側（JSON ファイル） |

---

## 3. グリッド仕様

- サイズ: 最大 **50×50 マス**
- 50×50 に収まらない場合は**エラーとして通知**し最適化を中断する

### レイヤー構造

グリッドは**輸送種別ごとのレイヤー**で構成される。

**現在の輸送種別（v1.1時点）:**

| レイヤーID | 名称 | 内容 |
|---|---|---|
| `conveyor` | コンベアレイヤー（地面） | 設備フットプリント、コンベア経路・ノード、電柱 |
| `pipe` | パイプレイヤー（空中） | 設備フットプリント、パイプ経路・ノード、電柱 |

> **拡張設計**: 輸送種別はデータ定義として管理し、コードを変更せずに新しい種別を追加できる設計とする（詳細は セクション 3-1 参照）。

### 各要素のレイヤー占有（現在）

| 要素 | conveyor レイヤー | pipe レイヤー |
|---|---|---|
| 設備 | ✅ | ✅ |
| コンベア経路・分岐・合流ノード | ✅ | — |
| コンベア交差ノード（自動生成） | ✅ | — |
| パイプ経路 | — | ✅ |
| パイプ分岐・合流・交差ノード | ✅（占有） | ✅ |
| 電柱 | ✅ | ✅ |

### 3-1. 輸送種別の定義モデル（拡張設計）

輸送種別はサーバー側に以下の構造で定義・保存する。新種別追加時はこの定義を追加するだけでシステムが対応できる設計とする。

```
TransportType {
  id:               string          // 例: "conveyor", "pipe"
  name:             string          // 表示名
  layer_id:         string          // 使用するグリッドレイヤーID
  occupies_layers:  string[]        // 分岐・合流ノード配置時に占有する追加レイヤーID
                                    // 例: pipe の分岐ノードは ["conveyor"] も占有
  can_coexist_with: string[]        // 通常経路が共存可能な他種別ID
                                    // 例: conveyor の直線は ["pipe"] と共存可
  display_order:    int             // ビューアの描画順（小さいほど下）
  color:            string          // 表示色
  line_style:       string          // 表示線種
}
```

**現在の定義例:**

| id | layer_id | occupies_layers（ノード時） | can_coexist_with |
|---|---|---|---|
| `conveyor` | `conveyor` | `[]` | `["pipe"]` |
| `pipe` | `pipe` | `["conveyor"]` | `["conveyor"]` |

---

## 4. 設備マスタ管理

### 4-1. 設備定義

- 名前・グリッドサイズ（幅×高さ、例：3×3、6×5）を登録・編集・削除
- 設備マスタはサーバー側に保存
- 4方向回転対応（0° / 90° / 180° / 270°）、ポートも回転に追従

### 4-2. ポート定義

各設備に輸送種別ごとの入出力ポートを定義できる。

| ポート種別 | 説明 |
|---|---|
| `{種別}` 入力ポート | その輸送種別が流入する接続点 |
| `{種別}` 出力ポート | その輸送種別が流出する接続点 |

> 新しい輸送種別が追加されると、その種別の入出力ポートも設備に定義できるようになる。

**制約:**
- ポートは**外周マスのみ**に配置可能
- ポートの向きは外周から**垂直方向（外向き）に固定**
- 各設備に同種ポートを複数持つことができる

GUI 上でポート位置をクリック指定して保存する。

### 4-3. プリセット設備

初期データとして以下を提供する：

| 名前 | サイズ | ポート構成 |
|---|---|---|
| 標準設備 | 3×3 | 上辺：コンベア入力、下辺：コンベア出力、左辺：パイプ入力、右辺：パイプ出力 |

---

## 5. フロー図エディタ

### 5-1. エディタモード

2 つのモードをトグルで切り替えて操作する：

| モード | 操作内容 |
|---|---|
| **ノード配置モード** | 設備ノード・分岐ノード・合流ノードをキャンバスに配置・移動・削除 |
| **接続モード** | ポートをクリックして輸送種別のエッジを接続 |

### 5-2. ノードの種類

輸送種別ごとに分岐・合流・交差ノードが存在する。新種別追加時は対応ノードが自動的に利用可能になる設計とする。

**現在のノード一覧:**

| ノード種別 | 配置方法 | 入力数 | 出力数 | グリッドサイズ | 占有レイヤー |
|---|---|---|---|---|---|
| 設備ノード | 手動 | マスタ定義 | マスタ定義 | マスタ定義 | 全レイヤー |
| コンベア分岐ノード | 手動 | 1 | 3 | 1×1 | conveyor のみ |
| コンベア合流ノード | 手動 | 3 | 1 | 1×1 | conveyor のみ |
| コンベア交差ノード | **自動生成** | 2（独立） | 2（独立） | 1×1 | conveyor のみ |
| パイプ分岐ノード | 手動 | 1 | 3 | 1×1 | pipe + conveyor |
| パイプ合流ノード | 手動 | 3 | 1 | 1×1 | pipe + conveyor |
| パイプ交差ノード | **自動生成** | 2（独立） | 2（独立） | 1×1 | pipe + conveyor |

> 交差ノードはルーティング時に経路が直交する箇所で自動生成される。交差点では合流・分岐は起きず、上下方向と左右方向が独立して通過する。

### 5-3. 接続操作

- 接続モードで**出力ポート → 入力ポート**の順にクリックしてエッジを作成
- 同種別の出力ポート → 同種別の入力ポートのみ接続可
- エッジには**流れの向き（方向）**があり、矢印で表示
- フロー図はループ構造を持たない（非循環グラフ）

### 5-4. フロー図の意味

フロー図は**論理的な接続関係のみ**を定義する。

- フロー図上のノードの座標はグリッドレイアウトに反映されない
- どのポートに接続するか・グリッド上のどこを経路とするかは**最適化エンジンが決定**

### 5-5. その他の操作

| 機能 | 内容 |
|---|---|
| Undo / Redo | フロー図エディタ上の操作履歴を戻す・やり直す |
| エクスポート | フロー図・レイアウト結果を JSON 形式で保存 |
| インポート | 保存した JSON を読み込んで再編集 |

---

## 6. 電柱

- サイズ: **2×2 マス**
- **電力供給範囲**: 電柱を中心とした **10×10 マスの正方形**
  - 実装上は電柱左上座標を基準に (-4, -4) 〜 (+5, +5) の範囲として定義
- **受電条件**: 設備のいずれか 1 マスでも供給範囲内に入れば電力供給済みとみなす
- 電柱は独立して機能（チェーン不要）
- **全レイヤーを占有**し、いかなる要素とも重複不可
- フロー図エディタでは配置しない（最適化エンジンが自動配置）
- **全設備が電力供給済みとなるよう電柱を自動配置**（最小本数）

---

## 7. 最適化エンジン

### 7-1. 動作モード

| モード | 説明 |
|---|---|
| 完全自動生成 | フロー図から設備配置・ノード配置・配管・電柱をすべて自動生成 |
| 手動配置 → 自動改善 | 手動でざっくり配置後に最適化ボタンで整理・圧縮 |
| 起点固定（ピン留め） | 特定設備の座標を固定したまま残りを最適化（ピン留め数の上限なし） |

最適化後も手動微調整が可能。

### 7-2. 最適化エンジンが決定する内容

| 決定事項 | 内容 |
|---|---|
| 設備のグリッド座標 | ピン留め設備以外は自由に配置 |
| 設備の回転 | 0° / 90° / 180° / 270° から選択 |
| 使用ポートの割り当て | 同種ポートが複数ある場合どれを使うかを選択 |
| 分岐・合流ノードのグリッド座標 | 1×1 ノードを最適位置に自動配置 |
| 配管経路 | A* アルゴリズムで自動ルーティング |
| 電柱のグリッド座標・本数 | 全設備をカバーする最小本数を自動配置 |

### 7-3. フロー図から引き継ぐ内容（固定）

| 固定事項 | 内容 |
|---|---|
| 論理接続グラフ | どのノードとどのノードが繋がるか |
| エッジ種別 | 輸送種別（conveyor / pipe / 将来追加種別） |
| ピン留め設備の座標 | 固定位置として扱う |

### 7-4. アルゴリズム（フェーズ 1）

**貪欲法 + 局所改善**

1. 接続グラフを起点から幅優先でたどり、接続関係が近い設備を近くに配置
2. ポート割り当てを決定
3. A* で各輸送種別の配管ルーティング（100 サイクルに 1 回更新）
4. 隣接設備のスワップ・回転変更を繰り返して外接矩形を縮小
5. 電柱の最小被覆配置を計算

> **フェーズ 2（将来）**: 焼きなまし法（Simulated Annealing）へ移行し、解の品質を向上させる

---

## 8. 配管ルーティング詳細

### 8-1. 基本ルール

- 各輸送種別はそれぞれのレイヤーで独立して A* ルーティング
- 曲がりは **90° のみ**（斜め接続不可）
- 経路・折れ曲がり・ノードにはすべて**流れの向き**がある

### 8-2. 重複・交差ルール

**設計原則**: すべてのノード・設備は外周でのみ接続される。経路がノード・設備の内部を通過することは不可。

**共通ルール（輸送種別に依らず適用）:**

| ケース | 可否 |
|---|---|
| 経路 / 折れ曲がりと設備の重複 | ❌ |
| 同種別の経路と分岐・合流ノードの重複 | ❌ |
| 同種別の折れ曲がりと別の経路・折れ曲がりの重複 | ❌ |
| 同種別の直線同士の直交交差（→ 交差ノード自動生成） | ✅ |
| 電柱といかなる要素の重複 | ❌ |

**種別間ルール（TransportType 定義の `can_coexist_with` / `occupies_layers` に基づく）:**

| ケース | 可否 |
|---|---|
| コンベア通常経路とパイプ通常直線経路の同一マス共存 | ✅ |
| コンベア分岐・合流ノードとパイプ通常経路の同一マス共存 | ✅ |
| パイプ分岐・合流・交差ノードとコンベア経路・折れ曲がりの同一マス | ❌ |
| パイプ分岐・合流・交差ノードとコンベア分岐・合流ノードの同一マス | ❌ |

> 新しい輸送種別追加時は `TransportType` の `occupies_layers` / `can_coexist_with` を定義することで重複チェックロジックが自動的に対応する。

### 8-3. マス状態モデル（実装参考）

```
GridCell {
  // 各レイヤーIDをキーとした辞書で管理（種別追加時も構造変更不要）
  layers: {
    [layer_id: string]:
      null
      | STRAIGHT(direction)
      | BEND(in_direction, out_direction)
      | BRANCH_NODE(transport_type_id)
      | MERGE_NODE(transport_type_id)
      | CROSSING_NODE(transport_type_id)
      | FACILITY
      | BLOCKED_BY_NODE(transport_type_id)  // 他種別ノードによる占有
      | POLE
  }
}
```

---

## 9. レイアウトビューア

### 9-1. 描画レイヤー（下から上の順）

描画順は `TransportType.display_order` に基づいて動的に決定する。

現在の順序（下から上）:
1. グリッド背景
2. コンベア経路・ノード（通常サイズ）
3. 設備フットプリント・ラベル
4. 電柱
5. パイプ経路・ノード（小さめ・上位レイヤー感を演出）
6. （将来追加種別）

### 9-2. 表示要素

- 設備: 種類・回転方向を視覚的に表示
- 各輸送種別: `TransportType` 定義の色・線種で区別、流れの向きを矢印で表示
- 分岐・合流ノード: 種別がわかるアイコン表示
- 電柱: 2×2 の範囲と供給範囲（10×10）をオプションで表示

### 9-3. 面積サマリー

外接矩形のサイズ・面積を常時表示（例: `12×9 = 108 マス`）  
電柱本数も表示（例: `電柱: 3 本`）

### 9-4. エクスポート

- レイアウト結果を JSON 形式でエクスポート
- （将来）画像エクスポート

---

## 10. 将来拡張（フェーズ 2 以降）

| 項目 | 内容 |
|---|---|
| 最適化アルゴリズム強化 | 焼きなまし法（Simulated Annealing）へ移行 |
| **新輸送種別の追加** | `TransportType` 定義を追加するだけで対応（コード変更不要） |
| 画像エクスポート | レイアウトビューアのスクリーンショット出力 |

---

## 11. 制約・前提事項

- フロー図はループ構造を持たない（非循環有向グラフ）
- 設備マスタの初期データ投入は手動（プリセット 1 種提供）
- エクスポート / インポートのバージョン互換性は考慮しない
- ピン留め設備の上限なし（最適化不能時は警告のみ）
- 配管・設備の数量（スループット）は考慮しない（接続関係のみ）
